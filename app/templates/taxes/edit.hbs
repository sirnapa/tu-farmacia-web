<div class="card card-edit">
    <div class="card-body">
        <div>
            {{#link-to "taxes.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "tax.title.edit"}}
            </h1>
        </div>

        {{partial "taxes/form"}}
    </div>
</div>
