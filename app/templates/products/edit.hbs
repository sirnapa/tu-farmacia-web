<div class="card card-edit">
    <div class="card-body">
        <div>
            {{#link-to "products.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "product.title.edit"}}
            </h1>
        </div>

        {{partial "products/form"}}
    </div>
</div>
