<div class="card card-new">
    <div class="card-body">
        <div>
            {{#link-to "tags.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "tag.title.new"}}
            </h1>
        </div>
        {{partial "tags/form"}}
    </div>
</div>
