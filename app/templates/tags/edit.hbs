<div class="card card-edit">
    <div class="card-body">
        <div>
            {{#link-to "tags.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "tag.title.edit"}}
            </h1>
        </div>

        {{partial "tags/form"}}
    </div>
</div>
