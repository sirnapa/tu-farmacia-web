{{#if showMenu}}
{{header-menu}}
{{/if}}

<main role="main" class="{{if showInContainer 'container'}}">
    {{outlet}}
</main>

{{#if showMenu}}
{{footer-menu}}
{{/if}}

{{ember-notify closeAfter=10000 messageStyle='bootstrap'}}
{{ember-load-remover}}
