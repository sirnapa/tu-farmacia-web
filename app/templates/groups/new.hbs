<div class="card card-new">
    <div class="card-body">
        <div>
            {{#link-to "groups.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "group.title.new"}}
            </h1>
        </div>
        {{partial "groups/form"}}
    </div>
</div>
