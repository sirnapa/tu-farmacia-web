<div class="card card-edit">
    <div class="card-body">
        <div>
            {{#link-to "groups.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "group.title.edit"}}
            </h1>
        </div>

        {{partial "groups/form"}}
    </div>
</div>
