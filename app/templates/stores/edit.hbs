<div class="card card-edit">
    <div class="card-body">
        <div>
            {{#link-to "stores.index" tagName="a" class="btn btn-back"}}
              {{t "button.back"}}
            {{/link-to}}
            <h1 class="card-title">
                {{t "store.title.edit"}}
            </h1>
        </div>

        {{partial "stores/form"}}
    </div>
</div>
